/* document.addEventListener('livewire:init', () => {
    Livewire.on('changes-published', (event) => {
        // window.location.reload();
        setTimeout(function() {
            window.location.href = '/admin';
        }, 999);
    });
    Livewire.on('model-changed', (event) => {
        window.location.reload();
    });
 }); */